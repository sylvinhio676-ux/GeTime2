1️⃣ Ouvrir un fichier (fopen)
FILE *f = fopen("data.txt", "r");

Modes principaux
Mode	Signification
"r"	lecture
"w"	écriture (écrase)
"a"	ajout
"r+"	lecture + écriture
"w+"	écriture + lecture

⚠️ Toujours vérifier :

if (f == NULL) {
    printf("Erreur ouverture fichier\n");
    return 1;
}

2️⃣ Écrire dans un fichier
Avec fprintf
FILE *f = fopen("data.txt", "w");

fprintf(f, "Nom: Alice\n");
fprintf(f, "Age: 25\n");

fclose(f);


👉 Écrit dans le fichier au lieu de l’écran.

3️⃣ Lire depuis un fichier
Lire ligne par ligne (fgets)
char ligne[100];
FILE *f = fopen("data.txt", "r");

while (fgets(ligne, sizeof(ligne), f) != NULL) {
    printf("%s", ligne);
}

fclose(f);

Lire mot / nombre (fscanf)
int age;
char nom[20];

FILE *f = fopen("data.txt", "r");
fscanf(f, "%s %d", nom, &age);
fclose(f);

4️⃣ Copier un fichier (exemple complet)
#include <stdio.h>

int main() {
    FILE *src = fopen("source.txt", "r");
    FILE *dest = fopen("copie.txt", "w");

    if (!src || !dest) {
        printf("Erreur fichier\n");
        return 1;
    }

    char c;
    while ((c = fgetc(src)) != EOF) {
        fputc(c, dest);
    }

    fclose(src);
    fclose(dest);
    return 0;
}

5️⃣ Lire / écrire caractère par caractère
Fonction	Rôle
fgetc	lire un caractère
fputc	écrire un caractère
6️⃣ Position dans le fichier
Se déplacer (fseek)
fseek(f, 0, SEEK_SET); // début
fseek(f, 0, SEEK_END); // fin

Position actuelle
long pos = ftell(f);

7️⃣ Supprimer / renommer un fichier
remove("data.txt");
rename("ancien.txt", "nouveau.txt");

8️⃣ Exemple réel : enregistrer des étudiants
#include <stdio.h>

typedef struct {
    char nom[20];
    int age;
} Etudiant;

int main() {
    FILE *f = fopen("etudiants.txt", "a");
    Etudiant e;

    printf("Nom: ");
    scanf("%s", e.nom);
    printf("Age: ");
    scanf("%d", &e.age);

    fprintf(f, "%s %d\n", e.nom, e.age);

    fclose(f);
    return 0;
}

🧠 Schéma mental
Programme C
   |
   |--> fopen()
   |--> fread / fwrite / fscanf / fprintf
   |--> fclose()

⚠️ Erreurs courantes

❌ Oublier fclose()
❌ Ne pas vérifier NULL
❌ Mauvais mode (r, w, a)

✨ Résumé express

FILE * = fichier

fopen / fclose

fprintf / fscanf

fgets / fgetc

Toujours vérifier les erreurs

Voici des fonctionnalités clés (pro, “production‑ready”) que tu peux ajouter pour rendre l’application plus complète et professionnelle :

Gestion fine des rôles et permissions (admin, super‑admin, enseignant, programmeur) avec restrictions d’accès par page et action.
Journal d’audit (qui a créé/modifié/supprimé quoi, et quand).
Export/Import (CSV/Excel/PDF) pour listes, emplois du temps et rapports.
Détection de conflits d’emplois du temps (salles, enseignants, horaires).
Notifications (email/SMS/in‑app) pour changements d’horaires, validations, rappels.
Rapports/Statistiques (taux d’occupation salles, charge enseignants, répartition filières).
Gestion documentaire (pièces jointes : syllabus, fiches de cours, programmes).
Workflow de validation (ex: programmation soumise → validée → publiée).
Mode multi‑campus/établissement avancé (filtres, espaces isolés).
Historique et versioning (rollback des programmations).
Sécurité renforcée (2FA, policies Sanctum, rate‑limiting).
Internationalisation (FR/EN), configuration globale par établissement.
Monitoring & logs applicatifs (erreurs, latences, disponibilité).
Tests automatisés + CI/CD (unitaires, intégration, e2e).
Sauvegarde/restore DB et gestion des migrations en prod.
Si tu veux, je peux te proposer une roadmap par priorité (MVP → Pro → Enterprise) et chiffrer l’effort estimé pour chaque bloc.

oui


Voici une roadmap par priorite (MVP → Pro → Enterprise) avec effort estimatif.

MVP+ (1–2 semaines)

Auth + roles de base (admin/teacher/programmer) : 1–2 j
Filtrage par campus/annee/niveau : 1–2 j
Gestion des conflits planning (salle/enseignant) : 3–4 j
Export CSV/Excel des listes : 1–2 j
Notifications in‑app simples (toasts + historique) : 1–2 j
Pro (3–5 semaines)

Workflow validation programmation (draft → valide → publie) : 4–6 j
Rapports/Stats (occupation salles, charge enseignants) : 4–6 j
Journal d’audit (who/when/what) : 3–5 j
Import Excel/CSV avec mapping : 4–6 j
Mode multi‑campus avance (espaces + permissions) : 4–6 j
Versioning simple des programmations : 3–5 j
Enterprise (6–10 semaines)

Notifications email/SMS (templates) : 5–8 j
2FA + securite (rate‑limit, policies) : 4–7 j
Internationalisation FR/EN : 3–5 j
Monitoring + alerting (logs, Sentry, uptime) : 4–6 j
CI/CD + tests e2e : 5–8 j
Sauvegarde/restore + plan de reprise : 4–6 j
Si tu veux, dis‑moi :

ta priorite (MVP/Pro/Enterprise)
ton delai
ton equipe (solo/2 pers/3+)
Et je te propose un plan detaille avec taches techniques et dependances.